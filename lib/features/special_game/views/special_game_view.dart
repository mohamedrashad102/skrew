import 'package:flutter/widgets.dart';

class SpecialGameView extends StatelessWidget {
  const SpecialGameView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}