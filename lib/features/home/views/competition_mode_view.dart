import 'package:flutter/material.dart';

class CompetitionModeView extends StatelessWidget {
  const CompetitionModeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('competitor')),
    );
  }
}
